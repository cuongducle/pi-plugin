{
  "name": "@cuongducle/pi-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use the earendil-works Pi coding agent from Claude Code.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "build": "node --check plugins/pi/scripts/pi-companion.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
